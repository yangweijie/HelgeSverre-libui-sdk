# 2026-07-07

## Chart component (libui Area self-draw, no 3rd-party chart lib)
Built a Chart.js-style chart component in `src/Chart/` (PSR-4 `Yangweijie\Ui2\` → `src/`).
- Types: Line, Bar, Pie, Doughnut, Scatter (`ChartType` enum; `isCartesian()` false for Pie/Doughnut).
- Files: ChartType, Dataset, Scale (nice-number ticks), ZoomState (domain + anchor zoom/pan/clamp), Animator (Loop::repeat tween, seekable for tests), ChartConfig (fluent + cross-platform font + DEFAULT_PALETTE), ChartView, ChartRenderer (interface), CartesianRenderer (abstract shared), LineRenderer, BarRenderer, PieRenderer, RendererFactory, Chart (extends AreaDelegate, public redraw()).
- Examples: `examples/chart-demo.php` (window 920x640, buttons: 折线/柱状/饼图/环形/散点/随机数据/重置缩放). Tests: `tests/ChartTest.php` (11 passing).
- Gesture-zoom emulation: libui Area exposes NO wheel/touch/drag events, only draw/mouse/mouseCrossed/dragBroken/key. Mapped: double-click = native `count===2`; pinch = Shift+drag (factor exp(-dx/pw*2.5)); pan = plain drag; keyboard +/-/= and 0 reset. `AreaMouseEvent.modifiers`: Shift=4, Ctrl=1.
- Polish: cartesian legend populated in CartesianRenderer::render; X tick labels center-aligned via `$gx - 40.0` offset.

## Verification this session
- `php85 -l src/Chart/CartesianRenderer.php` clean; `examples/chart-demo.php` clean.
- `php85 vendor/bin/pest tests/ChartTest.php` → 11 passed.
- Project path: /Volumes/data/git/php/HelgeSverre-libui-sdk (php85 = /Users/jay/Library/PhpWebStudy/alias/php85).
